# Copy PayrollEngine.Exchange.schema.json here
#
# Source: PayrollEngine main repo or any regulation repo (e.g. Regulation.COM.Base/Schemas/)
# This file is required for JSON $schema validation in editors and CI.
#
# Do not commit the schema file itself — it is maintained in the PE main repo.
# Add an entry to .gitignore if needed, or keep a local copy for development.
